meta {
  name: Delete Subscription
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/subscriptions
  body: json
  auth: none
}

body:json {
  {
    "endpoint": "https://fcm.googleapis.com/fcm/send/example-endpoint"
  }
}

docs {
  204 even if the endpoint was never registered — the end state the
  caller wants already holds.
}
