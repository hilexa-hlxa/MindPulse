meta {
  name: List Categories
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/categories
  body: none
  auth: none
}

docs {
  Categories are created implicitly by tagging a phrase — there's no
  separate create endpoint.
}
