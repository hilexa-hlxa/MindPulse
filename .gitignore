# OS / editor
.DS_Store
.vscode/
.idea/

# Local secrets left over from the old server build
.env
.env.local
.env.*.local
*.pem

# Local tooling
.venv/
node_modules/
*.log

# AI agent tooling — local state, session stores, and generated config. None of
# it is part of the app, and most of it is specific to one machine.
# CLAUDE.md is deliberately tracked: it documents the project, not the tooling.
.claude/
.claude-flow/
.agents/
.swarm/
.mcp.json
ruvector.db

# Other assistants, should any of them turn up here
.cursor/
.cursorrules
.aider*
.codex/
.gemini/
.windsurf/
.clinerules
